apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mock-rest.fullname" . }}-test-connection"
  labels:
    {{- include "mock-rest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mock-rest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
